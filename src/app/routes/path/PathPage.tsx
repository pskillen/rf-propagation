import SurfaceLayout from '../../components/layout/SurfaceLayout.tsx';

export default function PathPage() {
  return (
    <SurfaceLayout
      controls={null}
      canvas={<p>Path — point-to-point path surface arrives in phase 13.</p>}
    />
  );
}
