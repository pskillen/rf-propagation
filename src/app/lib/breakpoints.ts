/** Mantine `sm` — shared mobile/desktop breakpoint for v2 screen ports and chrome. */
export const DESKTOP_MIN_WIDTH_MEDIA_QUERY = '(min-width: 48em)';
export const MOBILE_MAX_WIDTH_MEDIA_QUERY = '(max-width: 48em)';
/** Mantine `md` — above typical tablet; AppLayout page content uses reduced centered width. */
export const ABOVE_TABLET_MIN_WIDTH_MEDIA_QUERY = '(min-width: 62em)';
